╔═══════════════════════════════════════════════════════════╗
║          RESEND API KEY ISSUE - FIX REQUIRED              ║
╚═══════════════════════════════════════════════════════════╝

ERROR: "This API key is restricted to only send emails"

Your Resend API key has SENDING ONLY permissions.
To add and verify domains, you need FULL ACCESS permissions.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

HOW TO FIX:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to https://resend.com/api-keys

2. DELETE your current API key

3. Create a NEW API key with FULL ACCESS:
   - Click "Create API Key"
   - Name: Levqor Backend
   - Permission: FULL ACCESS (not "Sending access")
   - Click Create

4. Copy the new key (starts with re_...)

5. Update in Replit:
   - Go to Secrets (🔐 icon in left sidebar)
   - Find RESEND_API_KEY
   - Click Edit
   - Paste new FULL ACCESS key
   - Save

6. Return here and run:
   python3 setup_resend_domain.py


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHY THIS MATTERS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

"Sending access" keys can ONLY send emails.
"Full access" keys can:
  ✅ Add domains
  ✅ Verify domains
  ✅ Get DNS records
  ✅ Send emails

For production, you can switch back to a "Sending access" key
after domain verification is complete.

