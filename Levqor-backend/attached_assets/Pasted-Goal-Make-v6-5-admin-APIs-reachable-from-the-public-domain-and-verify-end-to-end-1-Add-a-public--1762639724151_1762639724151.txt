Goal: Make v6.5 admin APIs reachable from the public domain and verify end-to-end.

1) Add a public routing shim in run.py
   - Register a catch-all that proxies /api/admin/<path:p> to /admin/<path:p>:
     from flask import request, Response
     @app.route("/api/admin/<path:p>", methods=["GET","POST","PUT","DELETE","PATCH","OPTIONS"])
     def compat_admin_proxy(p):
         # preserve method/body/headers; call the matching /admin/* view
         # simple internal forward using Werkzeug test_client to avoid HTTP roundtrip
         with app.test_client() as c:
             resp = c.open(f"/admin/{p}", method=request.method, data=request.get_data(), headers={k:v for k,v in request.headers if k.lower()!="host"})
         return Response(resp.get_data(), status=resp.status_code, headers=resp.headers)

   - Also mount the existing blueprints under a second prefix:
     app.register_blueprint(admin_insights_bp, url_prefix="/admin/insights")
     app.register_blueprint(admin_runbooks_bp, url_prefix="/admin/runbooks")
     app.register_blueprint(admin_postmortem_bp, url_prefix="/admin/postmortem")

2) CORS allow for levqor.ai:
   - Ensure CORS includes origins ["https://levqor.ai"] and path pattern ^/(api/)?admin/.*

3) Re-apply migration (idempotent) and restart:
   - sqlite3 levqor.db < db/migrations/007_insights.sql || true
   - restart the app

4) Public verification (use $API=https://api.levqor.ai):
   - curl -s $API/status
   - curl -s "$API/admin/runbooks"
   - curl -s "$API/admin/insights/brief/weekly" || curl -s "$API/api/admin/brief/weekly"
   - curl -s "$API/admin/insights/anomaly/explain?latency_ms=150" || curl -s "$API/api/admin/anomaly/explain?latency_ms=150"
   - curl -s -X POST "$API/admin/postmortem" -H "Content-Type: application/json" -d '{"incident":{"summary":"api timeout"}}'

5) Frontend sanity:
   - Visit https://levqor.ai/insights and https://levqor.ai/admin/insights
   - Confirm cards load without {"error":"internal_error"}.

6) Feature flags (keep enabled):
   - AI_INSIGHTS_ENABLED=true
   - SMART_OPS_ENABLED=true

7) Rollback plan:
   - If issues, remove the compat route and /admin/* prefixes, restart. No data changes.

Deliverables:
   - Code diff in run.py with the shim and blueprint prefixes
   - Verification log showing /admin/* and /api/admin/* both return 200
   - Note any remaining infra limitations.