You are Replit AI Agent working on the Levqor project.

You have FULL access to the code in this Repl.

Your job is to FINALIZE the GENESIS v8 frontend so that:
- The live site actually looks like a polished, dark, premium automation product
- The homepage has a clear “wow factor” with tiles, gradients, and proper copy
- The pricing matches the DONE-FOR-YOU model in GBP
- The sign-in page matches the Genesis v8 style
- Nothing in the backend or auth is broken

You must:
- VERIFY everything with real commands (curl, grep, npm run build, etc.)
- DO NOT assume “it should work” — always confirm in code / output
- At the end, show me which files you changed and what git commands I must run

======================================================================
PHASE 0 — CONTEXT + SAFETY RULES
======================================================================

0.1. Project layout assumptions:
- This is a Next.js app in:    ./levqor-site  (or current root if already there)
- App Router structure:        src/app/*
- Auth:                        NextAuth already configured (keep it)
- CSS:                         Tailwind CSS v3 with globals.css

If the app root is not clear, run:
- `ls`
- `ls src`
- `ls src/app`

0.2. SAFETY RULES (VERY IMPORTANT):
- DO NOT touch environment variables, Stripe keys, or NextAuth provider config.
- DO NOT change backend (Flask/Replit) or any API URLs.
- DO NOT delete existing pages (pricing, docs, etc.).
- ONLY modify frontend layout/styling/components as described.
- Always run `npm run build` after changes and fix any TypeScript/ESLint errors.

0.3. VERIFICATION RULES:
After changes you MUST:
- Run `npm run build` and ensure it completes successfully.
- Run `npm run lint` if available (but don’t fail the whole process if absent).
- Use `grep` or `rg` to confirm strings in the built HTML/TSX:
  - “Genesis v8.0”
  - “Automate work. Ship faster.”
  - “Self-healing automation”
- Summarize the output clearly for the user.

======================================================================
PHASE 1 — VERIFY TAILWIND CSS PIPELINE
======================================================================

1.1. Confirm Tailwind setup:

- Check that ALL of these exist and look correct:

  - tailwind.config.js
    - `content` must include:
      - "./src/app/**/*.{js,ts,jsx,tsx,mdx}"
      - "./src/components/**/*.{js,ts,jsx,tsx,mdx}"

  - postcss.config.js
    - Must export plugins: `{ tailwindcss: {}, autoprefixer: {} }`

  - src/app/globals.css
    - Must contain:
      - `@tailwind base;`
      - `@tailwind components;`
      - `@tailwind utilities;`

  - src/app/layout.tsx
    - Must import `./globals.css`.

1.2. If any of the above is missing or broken:
- Install Tailwind v3 + PostCSS (ONLY if not already present):
  - `"tailwindcss": "^3.4.17"`
  - `"postcss": "^8.4.49"`
  - `"autoprefixer": "^10.4.20"`
- Create/repair `tailwind.config.js` and `postcss.config.js` as needed.

1.3. Run:
- `npm run build`
- If it fails, fix all Tailwind/TS/JS issues until build is clean.

======================================================================
PHASE 2 — GLOBAL LAYOUT: DARK GENESIS THEME
======================================================================

TARGET FILE: `src/app/layout.tsx`

2.1. Requirements:

- Body must be a full dark background with slate colors:

  - `className="min-h-screen bg-slate-950 text-slate-50 antialiased"`

- Ensure that the layout:
  - Wraps children in a main container with proper padding.
  - DOES NOT duplicate a complex hero/header (homepage will handle that).
  - Keeps footer if already present, but adapt it to dark mode if necessary.

2.2. Actions:

- Open `src/app/layout.tsx`.
- Ensure:
  - It imports `./globals.css`.
  - `<body>` uses the dark style above.
  - Global nav, if present here, is very minimal OR moved to `page.tsx`
    so that the homepage can have a custom hero/nav.

- Keep SEO/metadata exports untouched.

2.3. After edit:
- Run: `npm run build`
- Fix any type/JSX errors.

======================================================================
PHASE 3 — HOMEPAGE: GENESIS v8 UI + TILES + PRICING CTA
======================================================================

TARGET FILE: `src/app/page.tsx`

3.1. OVERALL GOAL:

- The homepage must look like a premium automation SaaS:
  - Dark background
  - Genesis v8 badge
  - Clean nav
  - Hero section
  - 4 feature tiles
  - High-level pricing CTA (linking to /pricing, not touching Stripe logic)

3.2. HEADER REQUIREMENTS:

- Top of page: a sticky or static header with:
  - Left: Logo
    - Use existing gradient “L” logomark OR text “Levqor” styled with:
      - `text-lg font-semibold tracking-tight`
  - Center / Right: navigation links:
    - “Home”
    - “Pricing” (link to `/pricing`)
    - “Docs” (if route exists)
  - Right: “Sign in” (link to `/signin`)
  - CTA: “Start free trial” button

- Style:
  - dark background like `bg-slate-950/80 border-b border-slate-800/60`
  - `backdrop-blur` if you want it sticky at top.

3.3. HERO SECTION:

- Must include:

  - Small badge:
    - `Genesis v8.0`
    - Styled with emerald accent:
      - `inline-flex items-center rounded-full border border-emerald-500/40 bg-emerald-500/10 px-3 py-1 text-xs font-medium text-emerald-200`

  - Headline:
    - Text: `"Automate work. Ship faster."`
    - Class: `text-3xl sm:text-5xl font-semibold tracking-tight text-slate-50`

  - Subtext:
    - Mention self-healing automation:
      - `"Levqor runs your workflows, detects failures, and self-heals in minutes so you don't have to babysit your ops."`

  - Buttons:
    - Primary: “Start free trial”
      - `bg-emerald-500 hover:bg-emerald-400 text-slate-900 font-semibold rounded-lg px-6 py-3`
      - Link to `/pricing` OR future signup flow.
    - Secondary: “View demo”
      - `border border-slate-700 text-slate-100 hover:border-emerald-400/60 hover:bg-slate-900/60`
      - Can be a `<button>` for now or placeholder link.

3.4. GENESIS FEATURE TILES (4 TILES):

- Under the hero, create a 2x2 grid on desktop, stacked on mobile:

  Parent:
  - `className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"`

- Tiles:

  TILE 1:
  - Icon: 🔁
  - Title: “Self-healing workflows”
  - Text: “Detects failures automatically and retries, rolls back, or routes to backups.”
  - Classes:
    - `rounded-2xl bg-gradient-to-br from-emerald-500/20 to-blue-500/20 border border-emerald-500/30 p-5 hover:from-emerald-500/40 hover:to-blue-500/40 transition-all duration-300`

  TILE 2:
  - Icon: 🎨
  - Title: “Visual builder”
  - Text: “Design automations with a clean, visual canvas instead of 50 browser tabs.”
  - Gradient: violet-based.

  TILE 3:
  - Icon: 🧠
  - Title: “Intelligence engine”
  - Text: “Understands context, picks the safest path, and recommends improvements.”
  - Gradient: amber-based.

  TILE 4:
  - Icon: 🔗
  - Title: “Connect everything”
  - Text: “Email, Sheets, Drive, CRM, Stripe, webhooks, and your own APIs.”
  - Gradient: cyan-based.

3.5. CTA / TRUST SECTION:

- After tiles, add a section with:
  - Sentence like: “Built for operators, founders, and teams who can’t afford downtime.”
  - Optionally fake logos (placeholder text: “Used by teams like [Name1], [Name2]”) in muted grey.

- At bottom, add a final CTA:
  - Title: “Ready in 48 hours.”
  - Button: “See done-for-you pricing” → link to `/pricing`.

3.6. BUILD + VERIFY:

- Run:
  - `npm run build`
- If OK, then run:
  - `rg "Genesis v8.0" src/app/page.tsx`
  - `rg "Self-healing workflows" src/app/page.tsx`

======================================================================
PHASE 4 — SIGN-IN PAGE: MATCH GENESIS V8 THEME
======================================================================

TARGET FILE: `src/app/signin/page.tsx` (or /sign-in if that’s your route)

4.1. DO NOT TOUCH AUTH LOGIC:

- Do NOT change:
  - NextAuth providers
  - API routes
  - Callbacks
- Only change the layout and CSS classes.

4.2. DESIGN REQUIREMENTS:

- Full-page dark background:
  - Outer wrapper:
    - `className="min-h-screen bg-slate-950 flex items-center justify-center px-4"`

- Centered card:
  - `max-w-md w-full rounded-2xl bg-slate-900/70 border border-slate-800 p-8 shadow-xl backdrop-blur-xl space-y-6`

- Content:
  - Title: “Sign in to Levqor”
  - Small text: “Use your work email or a provider below.”
  - Buttons:
    - “Continue with Google” (primary)
    - “Continue with Microsoft” (secondary)
  - Optional disabled magic link section (e.g. input + disabled button “Magic link (coming soon)”).

- If the current implementation already wires Google/Microsoft, just wrap the existing sign-in handlers in nicely styled buttons.

4.3. Verify:

- Ensure the page compiles:
  - `npm run build`
- Grep:
  - `rg "Sign in to Levqor" src/app/signin/page.tsx`

======================================================================
PHASE 5 — PRICING STRATEGY IMPLEMENTATION (DISPLAY ONLY)
======================================================================

TARGET FILE: `src/app/pricing/page.tsx` (or wherever pricing lives)

IMPORTANT:
- DO NOT change Stripe product IDs or backend logic.
- Only adjust text/labels to match the chosen model.

5.1. PRICING MODEL TO IMPLEMENT:

We are using **Done-For-You one-time project tiers**, in GBP:

- Tier 1: “Starter — £99”
  - Copy:
    - “Perfect for your first automation.”
    - “One workflow, delivered in 48 hours.”
    - Bullets:
      - “1 workflow”
      - “Up to 3 tools (e.g. Email + Sheets + CRM)”
      - “Basic monitoring”
      - “Email support for 7 days”

- Tier 2: “Professional — £249”
  - Copy:
    - “For founders and teams who want reliability.”
    - “We design and ship your core automations in 2–4 days.”
    - Bullets:
      - “Up to 3 workflows”
      - “Up to 6 tools”
      - “Self-healing on critical steps”
      - “Priority support for 30 days”

- Tier 3: “Enterprise — £599”
  - Copy:
    - “When automation is mission-critical.”
    - “Deep integration, monitoring, and a 7-day build window.”
    - Bullets:
      - “Up to 7 workflows”
      - “Advanced routing and fallbacks”
      - “Monitoring dashboard”
      - “30 days of hands-on support”

5.2. ACTIONS:

- Find the current pricing page component.
- Adjust ONLY:
  - Tier names
  - Visible price text
  - Bullet points / descriptions
- DO NOT change any `priceId` / `stripePriceId` / environment variables.

5.3. Verify:

- `npm run build`
- `rg "Starter — £99" src/app/pricing -n` (or similar to confirm strings).

======================================================================
PHASE 6 — FINAL VERIFICATION + SUMMARY
======================================================================

6.1. Run one final build:

- `npm run build`

If build fails:
- Fix whatever the build log reports.
- Repeat until `npm run build` finishes successfully.

6.2. OPTIONAL local preview (if dev server allowed in this environment):
- `npm run dev` (only if appropriate and non-blocking).
- Otherwise, just rely on build + grep checks.

6.3. Summarize for the user:

At the very end, PRINT (in plain text):

1) A list of all files you modified, e.g.:

- src/app/layout.tsx
- src/app/page.tsx
- src/app/signin/page.tsx
- src/app/pricing/page.tsx
- tailwind.config.js (if needed)
- postcss.config.js (if needed)

2) The exact git commands the user must run to commit and push:

- `cd ~/workspace/levqor-site`
- `git status`
- `git add <list of files>`
- `git commit -m "feat(ui): Genesis v8 dark theme + tiles + DFY pricing"`
- `git push origin main`

DO NOT run these git commands yourself — just PRINT them.

3) A short checklist for the user to verify on production after Vercel deploys:

- Visit https://www.levqor.ai
  - Confirm dark background.
  - Confirm “Genesis v8.0” badge.
  - Confirm 4 Genesis feature tiles.
- Visit https://www.levqor.ai/signin
  - Confirm dark sign-in card with Google/Microsoft buttons.
- Visit https://www.levqor.ai/pricing
  - Confirm 3 tiers: £99 / £249 / £599 with the described copy.

======================================================================

Execute all phases carefully, verifying after each major change. 
Your goal: make the Levqor GENESIS v8 frontend look like a real, premium automation product that can convert visitors into paid DFY clients, WITHOUT breaking any backend or auth logic.