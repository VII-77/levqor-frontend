You are the Replit AI Agent. Create and deploy a Next.js marketing frontend for Levqor that consumes the existing Flask backend.

GOAL
- New Next.js app (App Router) with / and /pricing.
- Components: CTAButton (redirects to Stripe checkout), USPGrid, Testimonials, LiveStats, SubscribeForm.
- Uses backend assets + KPI API. Deploy to Vercel. Fail closed on any error.

ENV (set, do not print):
NEXT_PUBLIC_BACKEND_BASE=https://<BACKEND_DOMAIN>
NEXT_PUBLIC_BACKEND_CHECKOUT=${NEXT_PUBLIC_BACKEND_BASE}/billing/create-checkout-session
NEXT_PUBLIC_BACKEND_SUMMARY=${NEXT_PUBLIC_BACKEND_BASE}/api/v1/marketing/summary
NEXT_PUBLIC_ASSETS_BASE=${NEXT_PUBLIC_BACKEND_BASE}
NEXT_PUBLIC_APP_NAME=Levqor
# optional analytics:
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
NEXT_PUBLIC_HEAP_ID=

STEPS
1) APP SCAFFOLD
- Create new Next.js app in current repl folder `levqor-web/` (TypeScript, no ESLint).
- Files:
  levqor-web/marketing/landing_snippets.json  <- fetch at runtime from ${NEXT_PUBLIC_ASSETS_BASE}/marketing/landing_snippets.json
  levqor-web/marketing/testimonials.json      <- same pattern
  levqor-web/public/og-image.jpg (placeholder)
  levqor-web/src/components/CTAButton.tsx
  levqor-web/src/components/USPGrid.tsx
  levqor-web/src/components/Testimonials.tsx
  levqor-web/src/components/LiveStats.tsx
  levqor-web/src/components/SubscribeForm.tsx
  levqor-web/src/app/page.tsx
  levqor-web/src/app/pricing/page.tsx
  levqor-web/src/app/layout.tsx
  levqor-web/public/robots.txt
  levqor-web/public/sitemap.xml
- package.json: add "start": "next start -p $PORT -H 0.0.0.0".

2) COMPONENT LOGIC (minimal)
- CTAButton: onClick → POST to NEXT_PUBLIC_BACKEND_CHECKOUT with {plan} → window.location = json.url (handle 4xx).
- USPGrid/Testimonials: fetch JSON from NEXT_PUBLIC_ASSETS_BASE/marketing/*.json (SSR), render cards.
- LiveStats: fetch NEXT_PUBLIC_BACKEND_SUMMARY (ISR 60s). If missing keys, render nothing.
- SubscribeForm: POST email to ${NEXT_PUBLIC_BACKEND_BASE}/api/v1/newsletter/subscribe if exists; else store in localStorage and show “We’ll confirm by email.”

3) PAGES
- / (landing): headline + subhead from landing_snippets.json, <CTAButton plan="monthly" />, <USPGrid/>, <Testimonials/>, <LiveStats/>, <SubscribeForm/>.
- /pricing: three plans (monthly $19, annual $190, $1 test) using <CTAButton plan=...>.
- layout.tsx: meta description, OG/Twitter meta to /og-image.jpg; inject Plausible if NEXT_PUBLIC_PLAUSIBLE_DOMAIN; inject Heap if NEXT_PUBLIC_HEAP_ID; first-load ref capture of utm_* and ref to localStorage.

4) BUILD + VERIFY
- npm i
- npm run build
- npm run start (bind $PORT) and verify:
  200 / and /pricing; clicking CTA returns a Stripe checkout URL (don’t complete payment); view-source shows OG/Twitter tags; /sitemap.xml lists /, /pricing, /docs, /blog.

5) VERCEL DEPLOY
- Use Vercel CLI (token from env VERCEL_TOKEN if present). 
- vercel deploy --prod --yes
- Print deployed URL.

6) OUTPUT SUMMARY (print only)
{
 "frontend_url":"<vercel url>",
 "pages":["/","/pricing"],
 "kpi_bridge":"ok|missing",
 "assets_source": "${NEXT_PUBLIC_ASSETS_BASE}/marketing/*",
 "analytics":{"plausible":bool,"heap":bool}
}

SAFETY
- Do not modify backend code.
- Do not print secrets.
- Fail if any required page or CTA fetch fails.