You are the Replit AI Agent for the Levqor Frontend (levqor-frontend).

Your mission: Perform the HARDEST, MOST BRUTAL full-stack audit possible.

You MUST assume NOTHING is correct.  
Interrogate EVERY directory.  
Validate EVERY config.  
Check EVERY workflow.  
Inspect EVERY file that may break the build, break deployment, or break payments.

This is a READ-ONLY, FAILURE-SEEKING AUDIT.  
Do NOT modify or delete anything.

====================================================
🔥 SECTION 0 — ENTER THE PROJECT ROOT
====================================================

1. Identify the levqor-frontend folder:
   - Run: ls -la
   - If levqor-frontend exists and we are NOT inside it:
       cd levqor-frontend
   - Confirm with pwd and ls -la

2. Validate git repo:
   git rev-parse --is-inside-work-tree || echo "ERROR: Not inside a Git repo"

====================================================
🔥 SECTION 1 — FILESYSTEM & STRUCTURE AUDIT
====================================================

Audit for:
- Missing critical files
- Misplaced config files
- Duplicate configs
- Wrong casing (Next.js is case-sensitive in prod)
- Suspicious files left by agents/scripts
- Broken symlinks

Commands:
   find . -maxdepth 4 -type f | sort
   find . -xtype l -print || echo "No broken symlinks"
   ls -la .github
   ls -la .github/workflows
   ls -la public
   ls -la src
   du -sh *

====================================================
🔥 SECTION 2 — ENVIRONMENT VARIABLE AUDIT
====================================================

Search for ALL env vars referenced in the codebase:

   grep -R "process.env" -n . || echo "No env references found"
   grep -R "NEXT_PUBLIC_" -n . 
   grep -R "STRIPE_" -n .
   grep -R "AUTH_" -n .
   grep -R "_KEY" -n .
   grep -R "_SECRET" -n .

Report:
- Which env vars are used
- Which env vars are missing from Replit environment
- Which env vars look invalid or unused

====================================================
🔥 SECTION 3 — NEXT.JS CRITICAL CONFIG AUDIT
====================================================

Validate integrity of:
- package.json
- next.config.js
- tsconfig.json
- eslint config
- vercel.json (if exists)

Commands:
   cat package.json
   cat next.config.js || echo "No next.config.js"
   cat tsconfig.json
   cat vercel.json || echo "No vercel.json"

Check for:
- Missing scripts
- Incorrect build commands
- Conflicting Next.js settings
- Unsupported experimental flags
- Incorrect output mode
- Missing dependencies

====================================================
🔥 SECTION 4 — BUILD AUDIT (STRICT)
====================================================

Run strict local build:

   npm install
   npm run build 2>&1 | tee /tmp/build.log

If build fails:
   - Extract first 40 lines of error
   - Extract last 40 lines of error
   - STOP audit with "BUILD FAILED" flag

If build passes:
   - Print: "LOCAL BUILD: PASS"

====================================================
🔥 SECTION 5 — ROUTES, IMPORTS & DEAD CODE AUDIT
====================================================

Commands:
   grep -R "from '" -n src || grep -R "from \"" -n src
   grep -R "use client" -n src
   grep -R "export default" -n src
   grep -R "TODO" -n src
   grep -R "FIXME" -n src

Look for:
- Broken imports
- Unused components
- Dead routes
- Duplicate routes
- Missing pages
- Debug code
- Suspicious patterns

====================================================
🔥 SECTION 6 — STRIPE & PAYMENTS AUDIT
====================================================

Static audit only (no network).

Search for:
   grep -R "stripe" -n .
   grep -R "price_" -n .
   grep -R "checkout" -n .

Validate:
- All price IDs referenced
- Price IDs spelled correctly
- No leftover test-mode keys
- No missing secret env vars

====================================================
🔥 SECTION 7 — AUTH & SECURITY AUDIT
====================================================

Search for:
   grep -R "auth" -n .
   grep -R "session" -n .
   grep -R "NEXTAUTH" -n .
   grep -R "rate" -n .
   grep -R "limit" -n .
   grep -R "middleware" -n .

Check:
- API route protection
- Middleware presence
- Rate limiting config
- Public vs protected pages

====================================================
🔥 SECTION 8 — GIT / CICD / DEVOPS AUDIT
====================================================

Run:
   git status
   git branch -vv
   git remote -v
   git log --oneline -n 10

Inspect workflow files:
   sed -n '1,120p' .github/workflows/ci.yml
   sed -n '1,120p' .github/workflows/cloudflare-purge.yml

Check for:
- Misconfigurations
- Wrong triggers
- Missing steps
- Secrets mismatch
- Deprecated syntax

====================================================
🔥 SECTION 9 — FINAL AGGREGATED REPORT
====================================================

Produce a STRICT, BRUTAL report.  
For each section provide:

1. STATUS: OK / WARNING / FAIL  
2. Bullet list of ALL issues found  
3. Which issues block production  
4. Which issues are risky in future  
5. File paths where issues live  
6. A TOP-10 CRITICAL FIX LIST (ordered)

This MUST be clear, harsh, and 0% sugarcoating.

END OF INSTRUCTIONS.